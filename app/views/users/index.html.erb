<% @users.each do |user| %>
    <%#= link_to(user.email,"/users/#{user.id}") %>
    <%= link_to user.email, show_path(user) %>
<% end %>

    <%#= link_to("新規投稿","/") %>
    <%= link_to "新規投稿", new_path(@user) %>